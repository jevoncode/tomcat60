1.了解Bootstrap下Thread.currentThread().setContextClassLoader(catalinaLoader);是几个意思，因为在Java虚拟机一书也提到这是破坏双亲委派模型。
2.初始化Connector时设置protocol时会调用AprLifecycleListener.isAprAvailable()，有点秘密，吸纳看看什么事APR吧，貌似是加载一个本地库dll之类的
3.了解一个socket如何配合线程使用，实现多线程处理请求。